# 🗺️ خطة العمل — THE WAY OUT Agent

> مرتبة بالأولوية — تُحدَّث بعد كل جلسة عمل.

---

## 🔴 أولوية قصوى — التشغيل الأول على جهاز المستخدم

- [ ] فتح PowerShell والدخول لمجلد `the_way_out`.
- [ ] `pip install -r requirements.txt`.
- [ ] `python agent.py --check` → كل الفحوصات خضراء (عدا Ollama/الإنترنت حسب التوفر).
- [ ] `python agent.py` → التأكد من سطر `🔌 Ollama: متصل — الموديل جاهز ✅`.
- [ ] تجربة أمر عربي بسيط: `اكتب ملف hello.py يطبع السلام عليكم ثم اقرأه`.

**معيار القبول:** الإيجنت ينفذ أداتين على الأقل (`write_file` + `read_file`) ويرد بالعربي.

---

## 🟠 أولوية عالية — اختبار المهارات الحية

- [ ] `شغّل مشروع THE WAY OUT` → يفتح `http://localhost:5000` في المتصفح ويعرض الرسالة.
- [ ] `افحص المنفذ 5000` → يرد بأن الخدمة شغالة.
- [ ] `اقرأ موقع https://example.com ولخصه` → يعرض ملخصاً عربياً.
- [ ] `نفّذ أمر dir` → يعرض محتويات المجلد.
- [ ] إيقاف السيرفر عند الخروج من الإيجنت (تنظيف تلقائي).

**معيار القبول:** 4 من 5 مهارات تعمل من أول محاولة بالأوامر العربية.

---

## 🟡 أولوية متوسطة — أتمتة تصفح متقدمة (Playwright)

- [ ] تثبيت `playwright` + متصفح Chromium محلي.
- [ ] أداة `browse_jobs(site, query)`: بحث في موقع عمل حر وإرجاع قائمة مشاريع (JSON).
- [ ] فلترة النتائج (ميزانية، مهارات) + حفظها في `jobs.md`.
- [ ] ⚠️ **ممنوع التقديم التلقائي** — الاستخراج والعرض فقط إلا بموافقة صريحة.

**معيار القبول:** `ابحث عن مشاريع بايثون في موقع مستقل` يرجع قائمة من 5+ مشاريع حقيقية.

---

## 🟢 أولوية لاحقة — واجهة ويب (Web UI)

- [ ] `server.py` بـ FastAPI يعيد استخدام `agent.py` كـ core.
- [ ] صفحة شات عربية (RTL) + عرض خطوات الأدوات لحظة بلحظة (SSE streaming).
- [ ] زر إيقاف/تشغيل سيرفر THE WAY OUT من الواجهة.
- [ ] تبقى الـ CLI هي المرجع — أي ميزة في الـ UI يجب أن تعمل في CLI أولاً.

**معيار القبول:** نفس أوامر الأولوية العالية تعمل من المتصفح بنفس الجودة.

---

## 📝 سجل الجلسات

| التاريخ | ما تم | الخطوة الجاية |
|---|---|---|
| 2026-09-04 | تثبيت Ollama + تحميل `qwen2.5-coder:1.5b` + تجهيز `agent.py` الأولي | التشغيل الأول (`--check` ثم تفاعلي) |
| 2026-09-04 | بناء مستودع `THE-AGENT`: إصلاحات `agent.py` (v1.1) + أدوات جديدة + سكربتات + اختبارات + وثائق | المستخدم يحدّث مجلده المحلي من المستودع ويجرّب التشغيل الأول |
| 2026-09-05 | تثبيت Python 3.14.7 على جهاز المستخدم + التأكد من الموديل | نقل ملفات المشروع (تأجّل بطلب المستخدم لمراجعة الماستر برومبت أولاً) |
| 2026-09-05 | مراجعة الماستر برومبت (v10 ULTIMATE) وبناء `agent.py` **v2.0.0**: 39 أداة + قواعد ReAct السبع + ذاكرة دائمة + فريق وكلاء + تقرير مطابقة | المستخدم ينزّل ZIP الجديد وينقل الملفات ثم `pip install` ثم `--check` |
| 2026-09-05 | إصلاح الأدوات الـ3 المُكيّفة (**v2.1.0**): Frontier مجاني حقيقي + تحكيم battle + crontab حقيقي + dry-run — 50 اختباراً | المستخدم ينزّل ZIP الجديد (v2.1) وينقل الملفات ثم `pip install` ثم `--check` |
| 2026-09-05 | أول تشغيل حي: الموديل 1.5b كتب JSON كنص بدل tool calls (0 أدوات) ← بناء **v2.2.0** بمُنقذ JSON fallback + حد num_predict | المستخدم ينزّل ZIP الجديد (v2.2) ويعيد تجربة `hello.py` |
| 2026-09-05 | نجاح المُنقذ (write+read اتنفذوا ✅) لكن الموديل كرر read_file حتى 8 خطوات ← بناء **v2.3.0**: loop guard + ملخص تلقائي | المستخدم ينزّل ZIP الجديد (v2.3) ويعيد التجربة |
| 2026-09-05 | استقرار النظام (3 نسخ ناجحة ✅) لكن الموديل ادّعى تشغيل المشروع بـ 0 أدوات ← بناء **v2.4.0**: كاشف ادعاء + مطالبة بالتنفيذ | المستخدم ينزّل ZIP الجديد (v2.4) ويعيد `شغّل مشروع THE WAY OUT` |
| 2026-09-05 | الموديل كرر نفس الادعاء الكاذب 3 مرات متجاهلاً التحذيرين ← بناء **v2.5.0**: توجيه حتمي مباشر + depattern للبرومبت | المستخدم ينزّل ZIP الجديد (v2.5) ويعيد `شغّل مشروع THE WAY OUT` |
